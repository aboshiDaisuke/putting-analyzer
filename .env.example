# Google Gemini API key (free tier: https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_gemini_api_key_here

# All other variables are optional for local dev.
# When OAUTH_SERVER_URL is empty, auth is bypassed automatically.
# When BUILT_IN_FORGE_API_URL is empty, local storage (base64) is used.
